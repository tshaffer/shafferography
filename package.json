{
  "name": "shafferography",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node backend/dist/server.js",
    "canon-import": "npm --prefix backend run canon-import --",
    "build": "cd frontend && npm install && npm run build && cd ..",
    "heroku-postbuild": "cd backend && npm install && npm run build && cd ../frontend && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
